// 앱이 JS 실행을 시작한 시각. 네이티브 스플래시 노출 시간을 재는 기준점이다.
//
// 스플래시가 실제로 보인 시간은 '앱 아이콘을 누른 순간 ~ 스플래시를 내리는 순간'인데,
// 화면 컴포넌트가 마운트되는 시점은 이미 번들 로드가 끝난 뒤라 기준으로 쓸 수 없다.
// (기기·빌드 종류에 따라 그 사이가 수십 ms ~ 수 초까지 벌어진다)
//
// App.tsx 가 가장 먼저 import 하므로 이 값은 사실상 JS 시작 시각이다.
export const APP_START_MS = Date.now();
