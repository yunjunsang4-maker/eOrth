# Supabase 설정 — 이 파일을 .env로 복사한 뒤 실제 값을 넣으세요.
# 값은 Supabase 대시보드 > Project Settings > API 에서 확인합니다.
# 키를 넣은 뒤에는 `npx expo start -c` 로 캐시를 비우고 재시작해야 반영됩니다.
# (키가 비어 있으면 앱은 기존 모의 로그인으로 동작합니다)

EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
