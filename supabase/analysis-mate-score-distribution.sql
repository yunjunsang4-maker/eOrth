-- ============================================================
-- [폐기됨] 여행 DNA 매칭 점수 분포 분석 — 2026-07-28 이전 '4축 공식' 전용
--
-- ⚠️ 이 파일이 재던 점수 공식은 더 이상 존재하지 않는다.
--    측정하던 구 공식(합계 상한 100):
--      least(shared_count,5)*10  -- 겹친 나라     최대 50
--    + least(shared_comps,3)*5   -- 겹친 동행     최대 15
--    + least(shared_vts,2)*7     -- 겹친 기록형식 최대 14
--    + least(mutual_count,3)*7   -- 공통 메이트   최대 21
--
--    2026-07-28 재설계로 동행·기록형식 축이 제거되고, 나라 축은 개수가 아니라
--    희소성 가중 자카드가 됐다(장소·시의성·시기·관심사·성향·관계 6축).
--    → 이 파일을 그대로 돌리면 '없어진 공식'을 재서 "개선이 없다"는 잘못된 결론이 나온다.
--
-- 본문(공식 복제 + public._mate_score_pairs 뷰)은 삭제했다.
-- 새 공식을 여기에 다시 복제하지 않는 이유: schema.sql의 산식을 통째로 베끼는 순간
-- 같은 종류의 부패(공식이 바뀌면 조용히 어긋남)가 반복된다. 중복 로직 자체가 결함이다.
--
-- 새 점수 분포를 보려면 mate_suggestions를 직접 호출한다.
--   SQL 에디터(postgres 롤)에서는 auth.uid()가 NULL이라 항상 빈 결과가 나오므로,
--   실제 사용자 세션(앱/PostgREST) 또는 auth.uid()를 흉내 낸 요청으로 호출할 것.
--   예: select total_score, place_score, recency_score, season_score,
--              interest_score, taste_score, mutual_score
--       from public.mate_suggestions(50, '{}');
--   RPC가 직접 준 값이라 공식 복제 없이 축별 기여도까지 그대로 볼 수 있다.
--   (앱은 여기에 더해 로컬 기록의 나라 extra_countries를 넘긴다 — 실제 점수는 같거나 조금 높다.)
-- ============================================================

-- 과거 실행분이 남아 있을 수 있어 정리한다(구 공식 뷰는 유지할 이유가 없다).
drop view if exists public._mate_score_pairs;
